pipeline {
    agent any

    stages {
        stage('Running Tests') {
            steps {
                echo "======================================"
               echo "Test run is in progress"
                echo "====================================="
                echo "Run Completeed"
            }
        }
    }
}
